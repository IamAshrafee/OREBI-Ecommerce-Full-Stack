import React from "react";
import TopHeader from "../layout/TopHeader";
import MainWidth from "../layout/MainWidth";

const Home = () => {
  return (
    <MainWidth>
      <TopHeader />
      <div>home</div>
    </MainWidth>
  );
};

export default Home;
